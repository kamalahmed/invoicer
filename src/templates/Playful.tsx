import TemplateBase from './TemplateBase';
import type { TemplateProps } from './types';

export default function Playful(props: TemplateProps) {
  return <TemplateBase {...props} variant="playful" />;
}
